import { Vm } from 'store/vmStore';

interface Props {
  ip?: string;
  vm: Vm;
}

export function VirtualMachine({ ip, vm }: Props): JSX.Element {
  return (
    <p>
      <p>todo</p>
    </p>
  );
}
